from django.conf.urls import patterns, url

from ktv import views

urlpatterns = patterns('',
                       url(r'^$', views.detail_page, name='detail_page'),
                       url(r'^(?P<ktv_id>\d+)/$', views.detail_page, name='detail_page')
)




















